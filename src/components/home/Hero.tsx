export function Hero() {
  return (
    <section className="flex min-h-[calc(100svh-3.5rem)] items-center justify-center py-8 sm:min-h-[calc(100vh-3.5rem)] sm:py-12">
      <div className="text-center">
        <p className="tracking-tight text-white/60 text-[clamp(28px,6vw,60px)]">
          Justin
        </p>

        <h1 className="font-bold leading-[0.92] text-white text-[clamp(56px,14vw,128px)]">
          UCHENNA
        </h1>

        <p className="tracking-tight text-white/60 text-[clamp(28px,6vw,60px)]">
          Nwajei
        </p>
      </div>
    </section>
  );
}
